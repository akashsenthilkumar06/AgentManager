.PHONY: dev install check build

# Forward common commands to the project-level workflow so they work when the
# current terminal is inside frontend/.
dev:
	../.venv/bin/python ../scripts/dev.py

install:
	npm install

check:
	npm run check

build:
	npm run build

